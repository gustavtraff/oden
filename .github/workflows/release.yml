name: Build and Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'

jobs:
  # Build macOS DMG with bundled JRE and signal-cli
  # Build x86_64 using Rosetta on Apple Silicon runner - works on both Intel and Apple Silicon Macs
  build-macos:
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      # Install x86_64 Python via Rosetta for Intel-compatible build
      - name: Set up x86_64 Python via Rosetta
        run: |
          # Install Rosetta if not already installed
          softwareupdate --install-rosetta --agree-to-license || true
          
          # Download and install x86_64 Python
          curl -L -o python-installer.pkg "https://www.python.org/ftp/python/3.12.8/python-3.12.8-macos11.pkg"
          sudo installer -pkg python-installer.pkg -target /
          rm python-installer.pkg
          
          # Verify we have x86_64 Python
          /usr/local/bin/python3.12 --version
          file /usr/local/bin/python3.12

      - name: Get version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            SHORT_SHA=$(echo "$GITHUB_SHA" | cut -c1-7)
            echo "VERSION=snapshot-${SHORT_SHA}" >> $GITHUB_OUTPUT
          fi

      - name: Set version in source
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          sed -i '' "s/__version__ = \".*\"/__version__ = \"$VERSION\"/" oden/__init__.py
          cat oden/__init__.py

      - name: Install Python dependencies
        run: |
          # Use x86_64 Python
          arch -x86_64 /usr/local/bin/python3.12 -m pip install --upgrade pip
          arch -x86_64 /usr/local/bin/python3.12 -m pip install pyinstaller
          arch -x86_64 /usr/local/bin/python3.12 -m pip install -e ".[tray]"

      # Cache JRE download (x64 only for Intel build)
      - name: Cache JRE x64
        id: cache-jre-x64
        uses: actions/cache@v5
        with:
          path: build-dmg/jre-x64
          key: temurin-jre-25-macos-x64-v2

      - name: Cache signal-cli
        id: cache-signal-cli
        uses: actions/cache@v5
        with:
          path: build-dmg/signal-cli
          key: signal-cli-0.14.1-v1

      # Download JRE x64 (for Intel build - works via Rosetta on Apple Silicon)
      - name: Download JRE x64
        if: steps.cache-jre-x64.outputs.cache-hit != 'true'
        run: |
          mkdir -p build-dmg/jre-x64
          JRE_URL=$(curl -s "https://api.adoptium.net/v3/assets/latest/25/hotspot?architecture=x64&image_type=jre&os=mac&vendor=eclipse" | python3 -c "import sys, json; print(json.load(sys.stdin)[0]['binary']['package']['link'])")
          echo "Downloading JRE x64 from: $JRE_URL"
          curl -L -o jre-x64.tar.gz "$JRE_URL"
          tar -xzf jre-x64.tar.gz -C build-dmg/jre-x64 --strip-components=1
          rm jre-x64.tar.gz
          rm -rf build-dmg/jre-x64/man build-dmg/jre-x64/legal

      # Download signal-cli
      - name: Download signal-cli
        if: steps.cache-signal-cli.outputs.cache-hit != 'true'
        run: |
          mkdir -p build-dmg/signal-cli
          SIGNAL_CLI_VERSION="0.14.1"
          curl -L -o signal-cli.tar.gz "https://github.com/AsamK/signal-cli/releases/download/v${SIGNAL_CLI_VERSION}/signal-cli-${SIGNAL_CLI_VERSION}.tar.gz"
          tar -xzf signal-cli.tar.gz
          mv signal-cli-${SIGNAL_CLI_VERSION}/* build-dmg/signal-cli/
          rmdir signal-cli-${SIGNAL_CLI_VERSION}
          rm signal-cli.tar.gz
          chmod +x build-dmg/signal-cli/bin/signal-cli

      # Generate icons
      - name: Generate app icon
        run: |
          arch -x86_64 /usr/local/bin/python3.12 scripts/generate_icon.py
          arch -x86_64 /usr/local/bin/python3.12 scripts/generate_dmg_background.py

      # Copy dependencies to project root for PyInstaller
      - name: Prepare build files
        run: |
          cp -r build-dmg/jre-x64 .
          cp -r build-dmg/signal-cli .
          mkdir -p oden/static

      # Build with PyInstaller (x86_64 via Rosetta)
      - name: Build macOS app bundle
        env:
          ODEN_VERSION: ${{ steps.version.outputs.VERSION }}
        run: |
          arch -x86_64 /usr/local/bin/python3.12 -m PyInstaller --clean --noconfirm s7_watcher.spec

      # Create DMG
      - name: Install create-dmg
        run: brew install create-dmg

      - name: Create DMG
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          DMG_NAME="Oden-${VERSION}-macOS.dmg"

          # Try with background
          create-dmg \
            --volname "Oden ${VERSION}" \
            --volicon "images/oden.icns" \
            --background "images/dmg_background.png" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Oden.app" 150 190 \
            --hide-extension "Oden.app" \
            --app-drop-link 450 190 \
            "dist/${DMG_NAME}" \
            "dist/Oden.app" || true

          # Fallback if background fails
          if [ ! -f "dist/${DMG_NAME}" ]; then
            hdiutil create -volname "Oden ${VERSION}" \
              -srcfolder "dist/Oden.app" \
              -ov -format UDZO \
              "dist/${DMG_NAME}"
          fi

          ls -la dist/

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v7
        with:
          name: oden-macos-dmg
          path: dist/Oden-*.dmg

  # Build multi-arch Docker image and push to GitHub Container Registry
  build-docker:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Get version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            SHORT_SHA=$(echo "$GITHUB_SHA" | cut -c1-7)
            echo "VERSION=snapshot-${SHORT_SHA}" >> $GITHUB_OUTPUT
          fi

      - name: Set version in source
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          sed -i "s/__version__ = \".*\"/__version__ = \"$VERSION\"/" oden/__init__.py

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=snapshot-,enable=${{ !startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=snapshot,enable=${{ !startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Build Windows installer (Inno Setup) with bundled JRE and signal-cli
  build-windows:
    runs-on: windows-latest
    continue-on-error: true
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Get version
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            SHORT_SHA=$(echo "$GITHUB_SHA" | cut -c1-7)
            echo "VERSION=snapshot-${SHORT_SHA}" >> $GITHUB_OUTPUT
          fi

      - name: Set version in source
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.VERSION }}"
          $path = "oden/__init__.py"
          $content = Get-Content -Raw $path
          $content = $content -replace '__version__ = ".*"', "__version__ = `"$version`""
          Set-Content -Path $path -Value $content

      - name: Install Python dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller
          python -m pip install -e ".[tray]"

      - name: Cache JRE x64
        id: cache-jre-x64-win
        uses: actions/cache@v5
        with:
          path: build-win/jre-x64
          key: temurin-jre-25-windows-x64-v1

      - name: Cache signal-cli
        id: cache-signal-cli-win
        uses: actions/cache@v5
        with:
          path: build-win/signal-cli
          key: signal-cli-0.14.1-windows-v1

      - name: Download JRE x64
        if: steps.cache-jre-x64-win.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build-win/jre-x64 | Out-Null
          $apiUrl = "https://api.adoptium.net/v3/assets/latest/25/hotspot?architecture=x64&image_type=jre&os=windows&vendor=eclipse"
          $assets = Invoke-RestMethod -Uri $apiUrl
          $jreUrl = $assets[0].binary.package.link
          Invoke-WebRequest -Uri $jreUrl -OutFile jre-x64.zip
          Expand-Archive -Path jre-x64.zip -DestinationPath build-win/tmp-jre -Force
          $expandedDir = Get-ChildItem build-win/tmp-jre | Select-Object -First 1
          Move-Item "$($expandedDir.FullName)/*" build-win/jre-x64 -Force
          Remove-Item -Recurse -Force build-win/tmp-jre
          Remove-Item -Force jre-x64.zip

      - name: Download signal-cli
        if: steps.cache-signal-cli-win.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build-win/signal-cli | Out-Null
          $signalCliVersion = "0.14.1"
          $signalCliUrl = "https://github.com/AsamK/signal-cli/releases/download/v$signalCliVersion/signal-cli-$signalCliVersion.tar.gz"
          Invoke-WebRequest -Uri $signalCliUrl -OutFile signal-cli.tar.gz
          tar -xzf signal-cli.tar.gz -C build-win
          Move-Item "build-win/signal-cli-$signalCliVersion/*" build-win/signal-cli -Force
          Remove-Item -Recurse -Force "build-win/signal-cli-$signalCliVersion"
          Remove-Item -Force signal-cli.tar.gz

      - name: Generate app icon
        shell: pwsh
        run: |
          python scripts/generate_icon.py

      - name: Prepare build files
        shell: pwsh
        run: |
          Copy-Item -Path build-win/jre-x64 -Destination jre-x64 -Recurse -Force
          Copy-Item -Path build-win/signal-cli -Destination signal-cli -Recurse -Force
          New-Item -ItemType Directory -Force -Path oden/static | Out-Null

      - name: Build Windows app
        env:
          ODEN_VERSION: ${{ steps.version.outputs.VERSION }}
        shell: pwsh
        run: |
          python -m PyInstaller --clean --noconfirm s7_watcher.spec

      - name: Build installer
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.VERSION }}"
          iscc "/DMyAppVersion=$version" scripts/oden.iss

      - name: Upload Windows installer
        uses: actions/upload-artifact@v7
        with:
          name: oden-windows-installer
          path: Output/Oden-Setup-*.exe

  # Create release with all artifacts
  release:
    needs: [build-macos, build-docker, build-windows]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Download macOS artifacts
        uses: actions/download-artifact@v8
        with:
          name: oden-macos-dmg
          path: artifacts/oden-macos-dmg

      - name: Download Windows artifacts
        if: needs.build-windows.result == 'success'
        uses: actions/download-artifact@v8
        with:
          name: oden-windows-installer
          path: artifacts/oden-windows-installer

      - name: Get version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
            echo "IS_TAG=true" >> $GITHUB_OUTPUT
          else
            SHORT_SHA=$(echo "$GITHUB_SHA" | cut -c1-7)
            echo "VERSION=snapshot-${SHORT_SHA}" >> $GITHUB_OUTPUT
            echo "IS_TAG=false" >> $GITHUB_OUTPUT
          fi

      - name: List downloaded artifacts
        run: |
          echo "Downloaded artifacts:"
          find artifacts -type f -ls

      - name: Prepare release files
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          mkdir -p release_files
          
          # Copy macOS DMG
          find artifacts/oden-macos-dmg -name "*.dmg" -exec cp {} release_files/ \;

          # Copy Windows installer if available
          if [ -d artifacts/oden-windows-installer ]; then
            find artifacts/oden-windows-installer -name "*.exe" -exec cp {} release_files/ \;
          fi
          
          # Create source package for developers
          mkdir -p source_package
          cp -r oden source_package/
          cp -r obsidian-template source_package/
          cp -r docs source_package/
          cp pyproject.toml source_package/
          cp README.md source_package/
          cp CHANGELOG.md source_package/
          cp LICENSE source_package/
          cp Dockerfile source_package/
          cp docker-compose.yml source_package/
          
          # Set version in source
          sed -i "s/__version__ = \".*\"/__version__ = \"$VERSION\"/" source_package/oden/__init__.py
          
          cd source_package
          zip -r "../release_files/Oden-${VERSION}-source.zip" .
          cd ..
          
          # List release files
          echo "Release files:"
          ls -la release_files/

      - name: Delete previous snapshot release
        if: steps.version.outputs.IS_TAG == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Clean up legacy 'snapshot' tag/release (one-time migration)
          gh release delete snapshot --yes --cleanup-tag 2>/dev/null || true
          
          # Find and delete any existing snapshot-* releases (keep only the new one)
          gh release list --limit 20 --json tagName,isPrerelease -q '.[] | select(.isPrerelease and (.tagName | startswith("snapshot-"))) | .tagName' | while read -r tag; do
            echo "Deleting old snapshot release: $tag"
            gh release delete "$tag" --yes --cleanup-tag 2>/dev/null || true
          done
          sleep 5

      - name: Create Snapshot Release
        if: steps.version.outputs.IS_TAG == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          TAG="snapshot-$(echo '${{ github.sha }}' | cut -c1-7)"
          REPO="${{ github.repository }}"
          SCREENSHOTS_BASE="https://raw.githubusercontent.com/${REPO}/main/images"
          cat > /tmp/snapshot_notes.md << EOF
          ## ⚠️ Snapshot Build

          This is an automated snapshot build from the latest \`main\` branch.
          It is **not a stable release** and may contain untested changes.

          **Commit:** ${{ github.sha }}
          **Version:** ${VERSION}

          For stable releases, see the [latest release](https://github.com/${REPO}/releases/latest).

          ## Screenshots

          ### Dashboard
          ![Dashboard](${SCREENSHOTS_BASE}/dashboard.png)

          ### Setup Wizard
          ![Setup](${SCREENSHOTS_BASE}/setup.png)
          EOF

          gh release create "$TAG" \
            --title "Snapshot (${VERSION})" \
            --notes-file /tmp/snapshot_notes.md \
            --prerelease \
            --target ${{ github.sha }} \
            release_files/*

      - name: Create Versioned Release
        if: steps.version.outputs.IS_TAG == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release_files/*
          generate_release_notes: true
          body: |
            ## Installation
            
            ### macOS (Apple Silicon & Intel)
            1. Download `Oden-${{ steps.version.outputs.VERSION }}-macOS.dmg`
            2. Open the DMG and drag Oden to Applications
            3. First launch: Right-click → Open (to bypass Gatekeeper)

            ### Windows (Native Installer)
            1. Download `Oden-Setup-${{ steps.version.outputs.VERSION }}-x64.exe`
            2. Run the installer wizard (per-user install, no admin required)
            3. Launch Oden from Start Menu or desktop shortcut
            
            ### Docker (Linux, Windows, Raspberry Pi)
            ```bash
            docker pull ghcr.io/${{ github.repository }}:${{ steps.version.outputs.VERSION }}
            docker run -d \
              -p 8080:8080 \
              -v oden-data:/data \
              -v ./vault:/vault \
              ghcr.io/${{ github.repository }}:${{ steps.version.outputs.VERSION }}
            ```
            Or use `docker compose`:
            ```bash
            curl -O https://raw.githubusercontent.com/${{ github.repository }}/main/docker-compose.yml
            docker compose up -d
            ```
            
            ### From Source
            1. Download `Oden-${{ steps.version.outputs.VERSION }}-source.zip`
            2. Install signal-cli and Java 25+
            3. `pip install -e .` then `python -m oden`
            
            The web-based setup wizard will guide you through configuration on first run at `http://localhost:8080/setup`.
            
            ## Screenshots
            
            ### Dashboard
            ![Dashboard](https://raw.githubusercontent.com/${{ github.repository }}/main/images/dashboard.png)
            
            ### Setup Wizard
            ![Setup](https://raw.githubusercontent.com/${{ github.repository }}/main/images/setup.png)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}